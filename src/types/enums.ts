export enum Neo4jEncryption {
  ENCRYPTION_ON = 'ENCRYPTION_ON',
  ENCRYPTION_OFF = 'ENCRYPTION_OFF',
}

export enum OgmModel {
  Business = 'Business',
  Category = 'Category',
  Review = 'Review',
  User = 'User',
}

export enum UserRole {
  RoleAdmin = 'admin',
  RoleUser = 'user',
}
